# =========================
# KEYCLOAK CONFIG
# =========================
keycloak.auth-server-url=${KEYCLOAK_AUTH_SERVER_URL}
keycloak.realm=${KEYCLOAK_REALM}
keycloak.client-id=${KEYCLOAK_CLIENT_ID}
keycloak.client-secret=${KEYCLOAK_CLIENT_SECRET}

keycloak.admin.client-id=${KEYCLOAK_CLIENT_ID}
keycloak.admin.client-secret=${KEYCLOAK_CLIENT_SECRET}
keycloak.admin.username=${KEYCLOAK_ADMIN_USERNAME}
keycloak.admin.password=${KEYCLOAK_ADMIN_PASSWORD}

#spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8181/realms/women-concern
#
#keycloak.auth-server-url=http://localhost:8181
#keycloak.realm=women-concern
#keycloak.client-id=women-concern-api
#keycloak.client-secret=vw02pamd78B9hJXupmbVGPPDFaEbsVZK
#
#keycloak.admin.client-id=women-concern-api
#keycloak.admin.client-secret=vw02pamd78B9hJXupmbVGPPDFaEbsVZK
#keycloak.admin.username=admin
#keycloak.admin.password=Admin@women


# =========================
# DATABASE
# =========================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver


# =========================
# JPA
# =========================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect


# =========================
# SERVER
# =========================
server.port=9000
server.address=0.0.0.0
spring.application.name=women-concern-api

logging.level.org.springframework.security=DEBUG
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always


# =========================
# SECURITY (KEYCLOAK JWT)
# =========================
spring.security.oauth2.resourceserver.jwt.issuer-uri=${KEYCLOAK_ISSUER_URI}


# =========================
# BREVO
# =========================
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USERNAME}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.default-encoding=UTF-8

# Sender info
app.mail.from=manzi2020d@gmail.com
app.mail.from-name=Women Concern Management System



